// map() = return a new array with all the new values. You need to specify how to create these new values
// creating the new values can be done with the arrow function

let arr = [1, 2, 3, 4];
let mapped_arr = arr.map(x => x + 1); // each of the original array values has been increased by 1
console.log(mapped_arr);
