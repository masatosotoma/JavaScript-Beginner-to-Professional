// Math.expo() = to return the number to which e must be raised to get the input

let x = 2;
let exp = Math.exp(x);
console.log("Exp:", exp);

// Math.log() = to calculate the natural logarithm

let log = Math.log(exp); //it returns 7.3890---
console.log("Log:", log); // it return 2
