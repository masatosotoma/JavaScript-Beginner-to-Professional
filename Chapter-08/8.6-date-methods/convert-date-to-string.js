// toDateString() = to convert dates back to strings

let d = new Date();
console.log(d.toDateString());

// toLocaleDateString() = another method to convert dates to string with different format
console.log(d.toLocaleDateString());
