// join() method = to convert an array to a string

// example

let letters = ["a", "b", "c"];
let x = letters.join();
console.log(x);

//  If you want something else other than a comma for dividing, you can specify with parameter

let letters2 = ["a", "b", "c"];
let y = letters.join("-");
console.log(y);
