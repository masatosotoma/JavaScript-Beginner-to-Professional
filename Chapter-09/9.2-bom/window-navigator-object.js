/*
navigator property = an object of the window object which we specify with the dot in between
we access these window objects in the same way we do any other object
As navigator is also globally available, we can also access this without window in front of it with the command below
*/

console.dir(navigator);

// basically the information and the page control of the URL 