// to create a function that adds two numbers

function addingTwoNum(a, b) {
  return a + b;
}

// set up 2 variables with different values
let var1 = 30;
let var2 = 15;

// Use the function, output the result of addition of values
console.log(addingTwoNum(var1, var2));

// Use function and add arguments, do addition
console.log(addingTwoNum(30, 15));
