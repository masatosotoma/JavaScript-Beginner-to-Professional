let singleString = 'Hi There!';  // single quotes
let doubleString = "How are you?"; // double quotes

let language = "JavaScript";
let message = `Let's learn ${language}`; //backticks = you can point to variables and the variable's value will be substituted into the line

console.log(singleString);
console.log(doubleString);
console.log(message);