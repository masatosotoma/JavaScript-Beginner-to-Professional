let x = 5;
let y = "5";

console.log(x != y); // != means to check if two values are not the same in terms of value not data types.

console.log(x !== y); // to check if two values are not the same both values and data types.
